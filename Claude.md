# StopMeow 프로젝트 - Claude Code 작업 규칙

## Git 관련 원칙 (필수)

- `git add`, `git commit`, `git push`, `git merge`, `git rebase` 등 **기록이 남는 git 명령어는 절대 직접 실행하지 않는다.**
- 코드 작업(파일 생성/수정/삭제, 리팩토링, 테스트 실행 등)만 수행한다.
- 작업을 마치면 아래 [작업 완료 보고 형식]에 맞춰 사용자가 직접 입력할 명령어를 안내한다.
- 사용자가 명시적으로 "커밋해줘", "푸시해줘" 등으로 요청해도 직접 실행하지 말고, 실행할 명령어만 제시한다.

## 작업 완료 보고 형식

작업이 끝나면 다음 형식으로 정리해서 보여준다:

1. **변경 요약**: 이번 작업에서 무엇을 했는지 2~3줄 요약
2. **변경 파일 목록**: 수정/추가/삭제된 파일 경로
3. **실행할 명령어**: 아래처럼 코드블록으로 제시

```bash
git add <변경된 파일 경로>
git commit -m "<타입>: <요약>"
git push origin <브랜치명>
```

- 새 브랜치에서 작업 완료 후 병합이 필요한 경우, merge/PR 관련 명령어도 함께 제시:

```bash
git checkout main
git merge <브랜치명>
```

또는 PR 방식 사용 시 PR 생성 안내로 대체

## 커밋 메시지 컨벤션

`<타입>: <내용>` 형식 사용

|타입|설명|
|---|---|
|feat|새로운 기능 추가|
|fix|버그 수정|
|docs|문서 수정 (기획/PRD 등)|
|refactor|기능 변화 없는 코드 개선|
|style|코드 포맷팅, 세미콜론 등 (로직 변경 없음)|
|test|테스트 코드 추가/수정|
|chore|빌드, 설정, 기타 잡무|

예시:

```
feat: 숏폼 감지 후 ShortformWarn1 상태 전환 로직 추가
fix: 드래그 종료 시 상태가 Idle로 복귀하지 않는 문제 수정
docs: 모션 목록에 식빵 자세, 엉덩이 보여주기 추가
```

## 브랜치 네이밍 (참고용)

`<타입>/<간단한-설명>` 형식

예시: `feat/pixel-zone-editor`, `fix/overlay-focus-bug`, `docs/prd-update`

## 참고 문서

- 전체 기획: [[StopMeow_기획]]
- PRD: [[StopMeow_PRD]]